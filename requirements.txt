# Phase 1 dependencies. No model runtime yet -- MolmoWeb is still a stub, and
# adding torch/transformers before the format is known would be dead weight.
playwright>=1.48
pydantic>=2.7
pillow>=10.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24
