## Git
Never run git add, git commit, or git push. I make all commits myself.
When you finish a change, tell me which files to stage and suggest a
commit message. Do not run it.

## Secrets
.env holds PPAPI_KEY and PPAPI_BASE_URL. Read them from the environment
only. Never print, echo, cat, or log the key.
